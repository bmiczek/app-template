# Root environment variables (if needed)
# Most environment variables are configured in the individual app directories:
# - apps/backend/.env
# - apps/frontend/.env

# Global configuration can go here if needed
