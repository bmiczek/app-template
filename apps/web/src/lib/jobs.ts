/**
 * Background job workers placeholder.
 * Replace with actual job queue implementation (e.g. pg-boss) when needed.
 */

export async function startJobWorkers(): Promise<void> {}

export async function stopJobWorkers(): Promise<void> {}
